{{ name }} used the web form to contact us.

  AndroIRC version : {{ androirc_version|default('Unknown') }}
  Android  version : {{ android_version|default('Unknown') }}

{{ content|raw }}
--
AndroIRC
